/// <reference types="vite/client" />

// The installed Lucide package ships its runtime bundle; this declaration keeps
// builds robust in package-manager environments that omit its declaration file.
declare module 'lucide-react';
